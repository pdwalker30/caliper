{
  "eval_type": "code_review",
  "description": "Classic SQL injection in a user-lookup function. The username parameter is concatenated directly into the SQL query string. Severity: High. Remediation: use parameterized queries with placeholders.",
  "expected": {
    "vulnerability_kind": "sql_injection",
    "true_severity": "high",
    "true_severity_score": 0.85,
    "expected_remediation": [
      "Use parameterized queries (sqlite3 ? placeholders)",
      "Replace string concatenation with cursor.execute(query, (username,))",
      "Consider an ORM or query builder that parameterizes by default"
    ],
    "clarity_anchor": "A clear review names 'SQL injection', points to the line where 'username' is concatenated into the query string, and explains that an attacker can inject SQL by including special characters in the username."
  },
  "tags": ["sql", "injection", "auth", "high-severity"]
}
